version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8002:8002"
    env_file:
      - ./backend/.env
    volumes:
      - afu-data:/app/data
    restart: unless-stopped

volumes:
  afu-data:
